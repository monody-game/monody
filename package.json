{
    "private": true,
    "scripts": {
        "dev": "yarn run development",
        "development": "mix",
        "watch": "mix watch",
        "watch-poll": "mix watch -- --watch-options-poll=1000",
        "hot": "mix watch --hot",
        "prod": "yarn run production",
        "production": "mix --production",
        "start": "nodemon -L --watch /usr/src/app /usr/src/app/server.js"
    },
    "dependencies": {
        "dotenv": "^10.0.0",
        "jsonwebtoken": "^8.5.1",
        "socket.io": "^4.0.0"
    }
}
